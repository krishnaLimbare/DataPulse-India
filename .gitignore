__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# Derived data: regenerated from the parquet at deploy time by
# `datapulse summarize`. Tracking it made every nightly run conflict with any
# branch that had also run the pipeline.
dashboard/data/summary.json

# Local scratch
.DS_Store
datasets/_scratch/

# Kept out of git until deliberately added (git add -f). Swept in twice by
# an over-broad `git add -A`.
/Dockerfile
/docker-compose.yml

# Generated at deploy time from the committed parquet.
dashboard/data/*.csv
dashboard/data/schema.json
